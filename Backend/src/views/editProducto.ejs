<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar producto</title>
    <link rel="stylesheet" href="/css/agregarProducto.css">
    <link rel="shortcut icon" href="/img/logo.png" type="image/x-icon">
</head>

<body>
    <div class="container-add-producto">
        <h2>Editar producto</h2>
        <form action="/api/productos/admin/edit/<%= producto.id %>?_method=PUT" method="POST" class="form-add-producto">
            <div class="form-group">
                <label for="nombre">Nombre:</label>
                <input type="text" id="nombre" name="nombre"
                    value="<%= old && old.nombre !== undefined ? old.nombre : producto.nombre %>">
                <% if (typeof errors !=="undefined" && errors.length> 0) { %>
                    <% const errName=errors && errors.find(e=> e.type =="nombre");
                        if(errName){
                        %>
                        <p class="error" style="color: red;">
                            <%= errName.errMessage %>
                        </p>
                        <% } %>
                            <% } %>
            </div>
            <div class="form-group">
                <label for="precio">Precio:</label>
                <input type="number" id="precio" name="precio"
                    value="<%= old && old.precio !== undefined ? old.precio : producto.precio %>">
                <% if (typeof errors !=="undefined" && errors.length> 0) { %>
                    <% const errName=errors && errors.find(e=> e.type =="precio");
                        if(errName){
                        %>
                        <p class="error" style="color: red;">
                            <%= errName.errMessage %>
                        </p>
                        <% } %>
                            <% } %>
            </div>
            <div class="form-group">
                <label for="stock">Stock:</label>
                <input type="number" id="stock" name="stock"
                    value="<%= old && old.stock !== undefined ? old.stock : producto.stock %>">
                <% if (typeof errors !=="undefined" && errors.length> 0) { %>
                    <% const errName=errors && errors.find(e=> e.type =="stock");
                        if(errName){
                        %>
                        <p class="error" style="color: red;">
                            <%= errName.errMessage %>
                        </p>
                        <% } %>
                            <% } %>
            </div>
            <div class="form-group">
                <label for="imagen">Imagen</label>
                <input type="file" id="imagen" name="imagen">
                <% if (typeof errors !=="undefined" && errors.length> 0) { %>
                    <% const errName=errors && errors.find(e=> e.type =="img");
                        if(errName){
                        %>
                        <p class="error" style="color: red;">
                            <%= errName.errMessage %>
                        </p>
                        <% } %>
                            <% } %>
                                <% if (producto.imagen) { %>
                                    <div style="margin-top:8px;">
                                        <img src="/img/imgProductos/<%= producto.imagen %>" alt="Imagen actual"
                                            style="max-width: 120px; border-radius: 6px;">
                                    </div>
                                    <% } %>
            </div>
            <div class="form-group">
                <label for="tipo">Tipo actual: <%=producto.tipo%></label>
                <select id="tipo" name="tipo">
                    <option value="">Seleccionar tipo</option>
                    <option value="Accesorio" <%=(old && old.tipo==="Accesorio" ) || (!old &&
                        producto.tipo==="Accesorio" ) ? "selected" : "" %>>Accesorio</option>
                    <option value="Repuesto" <%=(old && old.tipo==="Repuesto" ) || (!old && producto.tipo==="Repuesto" )
                        ? "selected" : "" %>>Repuesto</option>
                </select>
                <% if (typeof errors !=="undefined" && errors.length> 0) { %>
                    <% const errName=errors && errors.find(e=> e.type =="tipo");
                        if(errName){
                        %>
                        <p class="error" style="color: red;">
                            <%= errName.errMessage %>
                        </p>
                        <% } %>
                            <% } %>
            </div>
            <div class="form-group">
                <label for="descripcion">Descripción:</label>
                <textarea id="descripcion" name="descripcion"
                    rows="3"><%= old && old.descripcion !==undefined ? old.descripcion : producto.descripcion %></textarea>
            </div>
            <button type="submit" class="btn-agregar">Guardar cambios</button>
        </form>
        <a href="/api/usuarios/admin/dashboard" class="btn-volver">Volver al dashboard</a>
    </div>
</body>

</html>